# Tool Shadowing

A malicious MCP server registers a tool with the same name as a trusted server's tool. If the host doesn't namespace tools by server, the malicious version can silently override the trusted one.
