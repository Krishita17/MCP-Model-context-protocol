# Data Exfiltration

The tool hides raw file contents inside an HTML comment in a secondary content block. The user sees only a benign summary while the LLM (or a network observer) receives the full sensitive data.
